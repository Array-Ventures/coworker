# Model Configuration
# Specify your preferred AI model using the format: provider/model-name
# Examples:
#   openai/gpt-4o-mini
#   anthropic/claude-sonnet-4-5-20250929
#   google/gemini-2.5-pro
#   groq/llama-3.3-70b-versatile
#   nvidia/meta/llama-3.3-70b-instruct
#   kimi-for-coding/k2p5
MODEL=openai/gpt-4o-mini

# API Keys (only needed for your chosen provider)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=
GROQ_API_KEY=
CEREBRAS_API_KEY=
MISTRAL_API_KEY=
NVIDIA_API_KEY=
KIMI_API_KEY=

# Agent Configuration
AGENT_ID=coworker

# Inngest (scheduled tasks)
INNGEST_DEV=1
INNGEST_BASE_URL=http://localhost:8288

# Database (PostgreSQL — defaults to localhost if not set)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Workspace Configuration (optional)
# Base path for all workspaces. /shared and /<agent-id> are created under this.
# WORKSPACE_PATH=/absolute/path/to/workspaces

# WhatsApp auth session storage (optional)
# WHATSAPP_AUTH_DIR=/data/whatsapp-auth

# Google (gog CLI) configuration
# GOG_HOME=/data/gog              # Config dir for gog CLI (defaults to $HOME)
# GOG_KEYRING_PASSWORD=            # Encrypts OAuth tokens at rest (required for VPS)
# GOG_GOOGLE_CLIENT_ID=            # Google OAuth client ID (enables Google account connection)
# GOG_GOOGLE_CLIENT_SECRET=        # Google OAuth client secret
